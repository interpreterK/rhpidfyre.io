{
  "name": "rhpidfyreio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm update && vite",
    "build": "npm update && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "typescript": "^5.0.2",
    "vite": "^4.3.0"
  },
  "dependencies": {
    "github-calendar": "^2.3.2",
    "sass": "^1.62.0"
  }
}
